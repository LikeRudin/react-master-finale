export const enum PATH {
  POPULAR = "/",
  NOWPLAYING = "/now-playing",
  COMINGSOON = "/coming-soon",
  DISNEY = "/disney",
  MARVEL = "/marvel",
}

export const enum LAYOUT_ID {
  CIRCLE = "circle",
}
